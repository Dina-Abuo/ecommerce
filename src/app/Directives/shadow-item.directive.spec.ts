import { SHadowItemDirective } from './shadow-item.directive';

describe('SHadowItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SHadowItemDirective();
    expect(directive).toBeTruthy();
  });
});
